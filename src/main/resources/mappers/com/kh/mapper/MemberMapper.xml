<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.mapper.MemberMapper">
	<insert id="insert">
		INSERT INTO 
		MEMBER(user_no,user_id,user_pw,user_name,job)
		VALUES(member_seq.NEXTVAL,#{userId},#{userPw},#{userName},#{job})
	</insert>

	<insert id="insertAuth">
		INSERT INTO MEMBER_AUTH VALUES(MEMBER_SEQ.CURRVAL,#{auth})
	</insert>

	<delete id="deleteAuth">
		DELETE FROM MEMBER_AUTH WHERE USER_NO = #{userNo}
	</delete>
	
	
	<select id="selectAll">
	
	</select>


</mapper>